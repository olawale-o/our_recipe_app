<table class="table">
<thead>
  <tr>
    <th scope="col">Food</th>
    <th scope="col">Measurement unit</th>
    <th scope="col">Unit price</th>
    <th scope="col">Actions</th>
  </tr>
</thead>
<tbody>
  <% foods.each do |food| %>
    <%= render partial: 'food_row', locals: { food: food } %>
  <% end %>
</tbody>
</table>